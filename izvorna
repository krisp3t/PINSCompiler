fun main(a: integer, b: integer): integer = (
    g(a, b)
) {where
    fun g(x: integer, y: integer):integer = (
        a + b + c + d
    ) {where
        var c:integer;
        var d:integer
    }
}