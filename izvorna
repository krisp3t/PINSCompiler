typ list: arr[10] integer;
var array: list;

fun main(x: integer): integer = (
  fill_array2(array)
);

fun fill_array2(a: list): integer =
  ({ for i = 0, 10, i + 1:
      ({array[i] = i * 5 },
      print_int(array[i]))
  }, 0)

{ where
  var i: integer
}
