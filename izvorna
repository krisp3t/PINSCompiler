fun fib(n:int):int = (
    # funkcija, ki vrne n-to fibonaccijevo stevilo
    { return = 0 },
    { if n == 1 | n == 2 then
        {return = 1}
    else (
        { return = 2 }
    )
    },
    return
) { where
    var return:int
};
typ int:integer;
typ str:string;
typ bool:logical